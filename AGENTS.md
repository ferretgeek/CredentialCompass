# Credential Compass 项目规则

- 先阅读工作区根 `AGENTS.md` 与本项目 `README.md`。
- 产品定位是隐私优先的 CLIProxyAPI 凭证池健康工作台，不是凭证导出器、任意请求代理、Codex 登录器或 OpenAI 官方产品。
- 原始 Token、管理密钥、上游响应正文、凭证文件、真实 ID、账号、私有地址与部署身份不得进入浏览器、磁盘、日志、截图、测试夹具或公开文档。账号默认遮罩，前端只接收匿名句柄与归类后的健康信息。
- 上游地址与管理密钥只能由部署者在启动环境中固定，禁止恢复网页编辑、动态目标、重定向跟随、任意探测 URL、凭证 ZIP、删除或批量导出。
- 默认只监听回环地址。非回环监听必须使用至少 32 字符的访问令牌与精确 Host 白名单；远程流量必须经过 HTTPS。不得放宽请求、响应、账号、并发、超时、同源、CSP 或速率限制。
- 兼容性实时探测必须保持默认关闭，并明确标注为非官方、可能失效的接口；任何兼容性变化都要更新中英文文档与测试。
- 状态变更必须保持默认关闭、最多 50 条、可逆且要求精确确认短语；禁止引入不可逆删除。
- UI 同步维护天青、青玉、夕照与深灰四套全局主题；深灰背景保持 `#17191d`。桌面与移动端均需实际渲染复核。
- 公开改动必须同步双语 README、真实界面截图、1280×640 社交预览、`docs/发布审计.md`、工作区根 `README.md` 与 GitHub 个人主页仓库。
